# Scripts

| Script | Role |
| --- | --- |
| `export_rl_token_policy_from_pt.py` | Export RL-token policy from a `.pt` checkpoint |

Prefer the installed CLIs (`rlt-so101-dual-*`) for day-to-day use; see the root `README.md`.
